// We import the CSS which is extracted to its own file by esbuild.
import "../css/app.css"

// Include phoenix_html to handle method=PUT/DELETE in forms and buttons.
import "phoenix_html"

// Import our custom board logic
import "./board.js"